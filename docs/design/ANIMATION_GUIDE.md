# Animation guide

Fast 120ms, normal 220ms, slow 360ms. Use transform/opacity for taps, sheets, messages, skeletons
and streaming cursor. Respect `prefers-reduced-motion`; never block interaction or rely on motion
to convey required state.
