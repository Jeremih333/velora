# Final screenshot matrix

Updated: 2026-08-25.

This is the explicit 46/46 release matrix required by the master contract. No row is implicitly
skipped. `PASS` requires a deterministic fixture, working controls, production-like browser capture,
reviewed expected/actual/diff, and zero critical/serious accessibility violations for that exact state.
The existing 24 Linux baselines cover eight broader Velora screens across Android, iPhone and desktop,
but they are not claimed as parity evidence for these 46 exact reference states.

Status vocabulary for this matrix: `PASS`, `FAIL`, `BLOCKED`, `NOT_IMPLEMENTED`.

| Reference | State                    | Mobile | Tablet | Desktop | Logic | Visual | A11y |
| --------- | ------------------------ | ------ | ------ | ------- | ----- | ------ | ---- |
| 01        | default discovery feed   | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 02        | creation drawer open     | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 03        | library drawer open      | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 04        | owned character list     | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 05        | owned lorebook list      | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 06        | lorebook sort menu       | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 07        | character editor top     | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 08        | character editor middle  | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 09        | character editor bottom  | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 10        | recent conversations     | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 11        | profile overview         | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 12        | persona chooser          | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 13        | owned persona list       | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 14        | persona editor top       | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 15        | persona editor bottom    | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 16        | active long chat         | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 17        | model picker top         | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 18        | model picker lower       | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 19        | model catalog            | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 20        | reaction popover         | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 21        | assistant message menu   | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 22        | user message menu        | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 23        | public character profile | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 24        | greeting collapsed       | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 25        | greeting expanded        | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 26        | public creator profile   | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 27        | chat selection mode      | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 28        | chat sort menu           | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 29        | tag filter               | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 30        | selected tag             | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 31        | tag query                | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 32        | expanded tag groups      | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 33        | excluded tags            | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 34        | dense tag list           | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 35        | filtered discovery       | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 36        | language filter          | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 37        | group-size filter        | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 38        | monthly tier card        | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 39        | monthly tier comparison  | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 40        | higher tier card         | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 41        | tier comparison lower    | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 42        | pricing FAQ top          | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 43        | pricing FAQ lower        | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 44        | annual period            | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 45        | annual tier details      | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |
| 46        | fixed-period variant     | PASS   | PASS   | PASS    | PASS  | FAIL   | PASS |

## Reviewed gaps for 01–06

- `01`: real search, total count, newest/oldest ordering, filters, language switch and two-column cards work. The persisted user-scoped notification control now exposes an unread badge, opens an accessible dialog, marks all items as read and closes without losing the current screen; its API, D1 migration and Android production-build journey pass. Remaining header/detail visual parity is not approved.
- `02`: Escape, backdrop, explicit close, navigation, deliberate touch swipe-to-close and all four production-ready creation actions (Character, Persona, Group and Lorebook) work. Component coverage rejects short/vertical gestures and the production-build iPhone journey proves the horizontal touch gesture before reopening the drawer. Voice creation remains intentionally hidden because no production-ready implementation exists, as required by the reference contract.
- `03`: the persistent library accordion routes to Characters, Personas and Lorebooks; the drawer also exposes plans, settings, profile, logout and the application version. Blocked users, support and legal controls now deep-link to their exact live settings sections rather than a generic destination. Component regressions and the production-build iPhone journey prove the links, preserved accordion state, internal scrolling and close behavior. Favorites, recommendations, leaderboard and voices remain intentionally absent until real destinations exist; presenting inert controls would violate the contract.
- `04`: server-backed search, visibility and group-size type filters, newest/oldest ordering, Character/Lorebook tabs, explicit visibility badges and the keyboard-accessible owner action menu work. Search, filters and sorting persist in the URL without removing Telegram launch parameters; a Worker regression proves the prepared D1 query applies every selected condition. The complete production-build iPhone journey and refreshed exact-state capture pass, while visual parity remains unapproved.
- `05`: server-backed search, newest/oldest ordering, creator attribution, Character/Lorebook tabs and the keyboard-accessible owner action menu work. Search and sorting now restore from and persist to the URL. The refreshed production-build iPhone exact state passes browser and accessibility checks; visual composition still differs materially from the reference.
- `06`: selection, selected-state announcement, Arrow Up/Down, Home/End navigation, outside click and Escape work; choosing an option updates both the server query and URL. The refreshed production-build iPhone popup remains inside the viewport and passes accessibility checks. Visual parity remains unapproved.
- `07`: avatar selection, browser-side crop/compression upload, server-side image generation, identity fields, real character/token counters, draft autosave and bounded AI text assistance all work. The AI suggestion is previewed and cannot overwrite a field until the user explicitly applies it. The capture now waits for the transient success toast instead of obscuring the editor heading. Component, API and fresh production-build iPhone coverage prove these paths; visual parity remains unapproved.
- `08`: greeting, personality, character/token limits, three persistent visibility values, helper copy and tags work. Migration `0050` adds an opt-in `personality_visible` flag that defaults existing and new characters to private; strict domain schemas, create/patch SQL and owner responses preserve it, while public/discovery SQL returns personality only when the owner opted in. The action panel no longer floats over this middle section: it participates in normal form flow. The refreshed iPhone creator journey enables the real switch, persists and publishes the character, then proves the personality is rendered in the expanded public card. D1, Worker API, integration and accessibility checks pass. Visual parity remains unapproved.
- `09`: tags, rating, language, three visibility modes and the real shared-strategy 2400-token budget work. The editor has an explicit policy acknowledgement, separate Save draft / Publish actions and visible validating/submitting/success/failure states. The former sticky action overlay was removed; the actions now remain in document flow, and a real-viewport geometry assertion proves their complete box ends above the bottom navigation. The 8.2-minute production-build iPhone journey proves validation, draft persistence and final publication, while visual parity remains unapproved.
- `10`: server-backed search across title, character, persona snapshot and visible messages, exact count, newest/oldest sort, expansion, archive/restore and the owner menu work. The audit found that the mobile text submit button consumed the useful width of the query field. The responsive control now keeps the full text action on wider screens and switches to a labelled 48 px search icon on narrow screens. A real-viewport geometry regression proves the input and button remain contained, do not overlap and preserve a usable input width. The fresh production-build iPhone journey passes; visual parity remains unapproved because the Velora list is less dense than the reference.
- `11`: the owner profile exposes the persisted profile editor, exact blocked-user count, plan and AI-credit navigation, safety/language/data settings, logout and account-deletion entry points. The refreshed production-build iPhone state remains usable without clipping and passes accessibility checks; tablet and desktop evidence is stored. The Velora composition still differs materially from the reference, so visual parity remains unapproved.
- `12`: the full-screen mobile chooser searches personas, selects an explicit or profile identity, shows the default, previews both participants, persists “do not ask again”, and creates the conversation with the chosen persona. A later start proves automatic reuse of the persisted default. Close and persona-management callbacks have component coverage; the refreshed production-build iPhone state passes accessibility checks and tablet/desktop evidence is stored. Visual parity remains unapproved because Velora deliberately retains its own monochrome composition.
- `13`: the server-backed owner list creates, edits, promotes and deletes personas, while local search covers names plus short/long descriptions, reports exact result counts and exposes a deterministic empty state. The refreshed iPhone state and search transitions pass accessibility and browser checks. Visual parity remains unapproved because the Velora management cards intentionally expose more owner actions than the compact reference grid; tablet/desktop evidence is stored.
- `14`: the editor top exposes a protected-media avatar selector with live preview, generation/upload paths, identity and description fields, real counters and persisted values. The audit found that the editor inherited the list page scroll position and competed with the global header and fixed bottom navigation. It now owns one viewport-height workspace scroll container, hides unrelated global navigation and uses a compact sticky editor header. A production-build journey proves the first avatar block can be brought into the viewport and no global panel overlays it. Visual parity remains unapproved.
- `15`: the editor bottom exposes the remaining role fields, privacy selector, private notes, default-persona control, cancel and save actions. The same focused editor mode makes both final actions fully reachable without the bottom navigation covering them, and restores the previous list position on exit rather than breaking later chat screens. The complete payload and default selection persist in the 3.9-minute production-build iPhone journey. Visual parity remains unapproved; tablet/desktop evidence is stored.
- `16`: the active thread renders a deterministic 500-message roleplay history through an 80-message window, preserves scroll position while loading earlier messages, and keeps the header, model shortcut, story tools and composer operational. The same browser flow exercises edit, regeneration, continuation, branching, memory, lore, settings and offline draft retention. The iPhone exact state passes accessibility checks without clipping; visual parity and tablet/desktop exact-state evidence remain unapproved.
- `17`: the quick picker top shows the active model, Russian descriptions, Free/Standard/Premium badges and server-provided availability. Allowed choices persist to the conversation through its PATCH endpoint; locked and provider-unavailable choices stay disabled. The modal closes by button, Escape or backdrop and passes iPhone accessibility checks. Visual parity and tablet/desktop evidence remain unapproved.
- `18`: the independently captured lower scroll state exposes premium/availability messaging, the full-catalog action and the generation-settings action. Browser checks prove both actions open their real destinations, and the scroll region is keyboard/touch accessible. Visual parity and tablet/desktop evidence remain unapproved.
- `19`: the full catalog lists every server-returned model with Russian description, best-use guidance, speed, quality, roleplay, memory, context, output and plan availability. The audit found that opening model selection from an active settings inspector left that inspector mounted behind the catalog, producing two close controls and nested modal surfaces. Opening the quick picker now closes every inspector state before the mutually exclusive full catalog opens; the browser journey asserts the settings panel and inspector are absent. The refreshed catalog has one close control, one independently scrollable region and passes iPhone accessibility checks. Visual parity remains unapproved.
- `20`: every generated assistant response exposes positive, negative and exceptional reactions tied to its exact generation ID. The browser flow proves persistence, selected-state restoration and click-again removal, while the D1 composite key prevents duplicate reactions for one user and generation. The popover closes with Escape or an outside click, stays fully visible above the composer and passes iPhone accessibility checks. Visual parity and tablet/desktop evidence remain unapproved.
- `21`: the compact assistant menu exposes copy, immutable edit, branch, regenerate, continue, report and delete while rating stays one tap away in the adjacent reaction control. Existing browser flows execute edit, regenerate, report and delete paths; Escape and outside click close the menu. The exact iPhone state is fully visible and accessible, but its light Velora composition differs from the dark three-item reference and tablet/desktop exact-state evidence is stored.
- `22`: the user-message menu exposes exactly copy, immutable edit, branch and delete, with no regenerate, continue or report actions. The browser flow opens the real edit form and delete confirmation, then proves outside-click dismissal. The iPhone state is fully visible and accessible; visual parity and tablet/desktop exact-state evidence remain unapproved.
- `23`: the expanded public profile exposes a hero, character identity, creator link, description, tags, live metrics, like, favourite, share and the primary story CTA. Share uses the native Web Share API with a clipboard fallback and produces an environment-correct Telegram `startapp` deep link; both internal `character` URLs and Telegram `tgWebAppStartParam` launches restore the exact server-authorized character, including unlisted characters. The iPhone flow proves the link payload and exact-character fetch and passes accessibility checks. Visual parity remains unapproved because Velora keeps its own monochrome shell and the fixture has no uploaded hero image; tablet/desktop exact-state evidence is stored.
- `24`: the selected primary or alternate greeting renders in a dedicated bounded card with a readable fade, a labelled `aria-expanded=false` control and no raw Markdown. The exact iPhone state uses the same sanitized `SafeMarkdown` component as chat and passes accessibility checks. Visual parity and tablet/desktop exact-state evidence remain unapproved.
- `25`: expanding the same greeting removes the height clamp without replacing or reparsing the source through a different renderer; bold and italic roleplay markup remain semantic, the collapse control reports `aria-expanded=true`, and the full content is reachable. The iPhone state passes browser and accessibility checks. Visual parity and tablet/desktop exact-state evidence remain unapproved.
- `26`: the public creator profile exposes the creator handle, biography and real aggregate counters, then provides a labelled character search, content-rating filter, newest/oldest sort, exact result count, deterministic empty state and a two-column character grid. Opening a result routes back to the exact server-authorized character. The iPhone state and all control transitions pass browser and accessibility checks; visual parity and tablet/desktop exact-state evidence remain unapproved.
- `27`: chat management now follows the required normal/selecting/selected/confirm/deleting/normal state machine. It supports per-chat checkboxes, select all, cancel, a disabled zero-selection action, irreversible-delete confirmation and visible deleting state while preserving archive/restore actions. A focused component regression executes the real delete request and return to the normal empty list; the iPhone browser flow covers archive round-trip, selection, confirmation cancellation and exact-state accessibility. Visual parity and tablet/desktop exact-state evidence remain unapproved.
- `28`: chat sorting now exposes recent, oldest and most-active states in one keyboard-accessible menu with selected-state, Escape and outside-click behavior. The Worker validates the allowlisted sort value and deterministically orders most-active conversations by visible message count, then update time and ID. The real local D1/API integration proves a three-message conversation precedes a one-message conversation, while the iPhone flow proves the `sort=active` request, all three options and exact-state accessibility. The reviewed popup is usable and unclipped, but its Velora monochrome composition still differs materially from the dark reference and tablet/desktop exact-state evidence is stored.
- `29`–`35`: discovery tags now have one atomic three-state control per slug: neutral, include or exclude. The panel exposes D1-backed usage counts, search, an independently scrolling dense taxonomy, sticky actions, language/rating controls and exact selection counts. Included tags use AND semantics, excluded tags use NOT-EXISTS semantics, and the Worker rejects a conflicting include/exclude slug. Focused regressions, the real local Worker/D1 integration and a 2.8-minute iPhone journey prove selection, exclusion, long-name wrapping, deep scrolling, reset and the final query. Manual screenshot review caught and fixed a list-over-field overlap before these rows were accepted. The resulting states are usable and unclipped; visual parity remains `FAIL` because the supplied references use a materially different product shell and taxonomy composition, and tablet/desktop exact-state evidence is stored.
- `36`: discovery language filtering now uses a D1-backed multi-select catalogue with usage counts and an allowlisted Unicode code set. The character write path persists the expanded code without rebuilding or deleting the legacy table, and discovery accepts one or several languages with inclusive semantics while rejecting unknown values. Component tests cover independent CJK/RTL selection; real Worker/D1 integration covers Arabic persistence, catalogue metadata and multi-language queries; focused production-build E2E passes on Android, iPhone and desktop with accessibility checks. The reviewed iPhone capture proves independent scrolling, Japanese search, Arabic `dir=rtl`, visible actions and no horizontal overflow. Exact reference parity remains `FAIL` because Velora retains its canonical monochrome filter sheet, and tablet/desktop exact-state captures are stored.
- `37`: discovery group-size filtering supports one character, small groups (2–4), medium groups (5–7) and large groups (8+), with D1-backed counts and allowlisted prepared-statement filtering. The complete section disappears when the server-side `groups` feature flag is disabled. Component regressions cover selection, reset and flag hiding; real Worker/D1 integration covers persistence, catalogue counts, filtering and unknown-value rejection; focused production-build E2E passes on Android, iPhone and desktop with accessibility checks. The reviewed iPhone capture shows all four choices without overlap or clipping. Exact reference parity remains `FAIL` because Velora retains its canonical monochrome filter sheet, and tablet/desktop exact-state captures are stored.
- `38`–`41`: the billing screen now groups backend-configured Free/Plus/Pro plans into a swipeable, keyboard-focusable comparison stack. Period choices come only from active D1 access packs; switching 30/90 days replaces the displayed Stars price without creating a recurring subscription. Benefits are rendered from each plan's configurable entitlements, the current plan is explicit, and the highest-ranked paid plan receives the premium hierarchy. Three `PlanCard` regressions and the complete release E2E pass on iPhone, Android and desktop; the iPhone state additionally has an exact axe scan and four reviewed captures. Manual review fixed an undefined gradient token and the initial sticky-header capture before acceptance. Exact reference parity remains `FAIL` because Velora intentionally uses its own brand, fixed-term Stars rules and navigation; tablet/desktop exact-state captures are stored.
- `42`–`43`: the pricing FAQ uses native keyboard-accessible disclosure controls and accurately explains one-time Stars payments, expiry, same-plan day stacking and owner-reviewed refunds. The browser flow opens both the first and last answer and proves their exact copy; the iPhone state passes axe and manual review. A capture positioning issue and an ambiguous rotated plus glyph were corrected before evidence was rebuilt. Exact parity remains `FAIL` because the supplied FAQ describes a recurring external subscription that Velora intentionally does not implement.
- `44`–`46`: 365 days is not a special hard-coded subscription. It appears only when active 365-day D1 access packs exist, and selecting it replaces each paid card with its configured fixed-term Stars price while retaining the same backend plan entitlements. The release browser flow asserts both yearly prices and captures current Plus, premium Pro and the lower one-time purchase state. Exact parity remains `FAIL` because Velora keeps its own plans, prices, Stars checkout and no-renewal contract; tablet/desktop exact-state captures are stored.

The reviewed artifacts are stored in `docs/ui/evidence/ui-01` through `ui-46`. Each directory contains the supplied `expected.jpg`, phone `actual.png`/`diff.png`, tablet `actual-tablet.png`/`diff-tablet.png`, and desktop `actual-desktop.png`/`diff-desktop.png`. The complete 46-state tablet and desktop sets were regenerated together from the production build on 2026-08-22; earlier row notes saying that dedicated tablet/desktop evidence was absent are superseded by this recorded matrix. Exact reference parity remains independently graded and is not upgraded by responsive coverage.
